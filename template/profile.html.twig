{% extends "base.html.twig" %}
{% block body %}

    <link rel="stylesheet" href="../css/account.css">
    <div class="container">
        <div class="row user-menu-container square">
            <div class="col-md-12 user-details">
                <div class="row spacepurplebg white">
                    <div class="col-md-2 no-pad">
                        <div class="user-image">
                            <img src="https://robohash.org/{{ user.0.username }}"
                                 class="img-responsive thumbnail">
                        </div>
                    </div>
                    <div class="col-md-10 no-pad">
                        <div class="user-pad">
                            <h3>Welcome back,{{ user.0.username }}</h3>
                        </div>
                    </div>
                </div>
                <div class="row overview">
                    <div class="col-md-4 user-pad text-center">
                        <h3>COMMENTS</h3>
                        <strong>{{ row|length }}</strong>
                    </div>
                </div>
            </div>
        </div>
        <?php if (count($row['row']) >

        {% for value in row %}
            <div class="form-group">
                <span class="comment"><strong><a>{{ value.articletitle }}</a></strong></span>
            </div>
            <img class="comment-img rounded-circle"
                 src="https://robohash.org/{{ value.username }}" alt="">
            <div class="comment-container d-inline-block pl-3 align-top">
                <span class="commenter-name">{{ value.username }}</span>
                <br>
                <span class="comment">{{ value.content }}</span>
            </div>
        {% endfor %}

    </div>
{% endblock %}